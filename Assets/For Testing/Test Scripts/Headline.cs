public class Headline
{
    public string Text;
    public bool IsReal;

    // Constructor to set the headline's properties
    public Headline(string text, bool isReal)
    {
        Text = text;
        IsReal = isReal;
    }
}